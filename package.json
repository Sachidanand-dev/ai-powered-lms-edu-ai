{
  "name": "ai-powered-lms",
  "version": "1.0.0",
  "description": "AI Powered LMS Deployment Root",
  "scripts": {
    "install-client": "cd client && npm install",
    "install-server": "cd server && npm install",
    "build-client": "cd client && npm run build",
    "start": "node server/index.js",
    "render-postbuild": "npm run install-client && npm run install-server && npm run build-client"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}